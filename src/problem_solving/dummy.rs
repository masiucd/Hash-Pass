use crate::util_fns::helpers::print_name_if_even_age;


pub fn print_name_if_even_age(age: u8, name: &str) {
    let predicate = create::
    if predicate {
        println!("{}", name);
    }
    println!("not even");
}
